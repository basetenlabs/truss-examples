{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Restart Server",
            "type": "shell",
            "command": "kill $(pgrep uvicorn) 2>/dev/null || true; sleep 1; cd /app/data && uv run uvicorn server:app --host 0.0.0.0 --port 8090 --reload",
            "group": { "kind": "build", "isDefault": true },
            "presentation": { "reveal": "always", "panel": "dedicated" }
        }
    ]
}
