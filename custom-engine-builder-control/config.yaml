# this file was autogenerated by `generate_templates.py` - please do change via template only
model_metadata:
  example_model_input:
    max_tokens: 100
    messages:
      - content: You are a helpful assistant. To each math question, e.g. <Whats 1+1>, respond with all parts the question, '=', and answer.
        role: system
    stream: false
    suffix_messages: [
        # k=1
        [{ "role": "system", "content": "Whats 1+1" }],
        # k=2
        [{ "role": "system", "content": "Whats 2+2" }],
      ]
    temperature: 0.5
    chat_template_kwargs: { "enable_thinking": false }
  tags:
    - openai-compatible
model_name: Briton-suffix-fanout-qwen3-8B
python_version: py39
resources:
  accelerator: H100
  cpu: "1"
  memory: 10Gi
  use_gpu: true
trt_llm:
  build:
    base_model: decoder
    checkpoint_repository:
      repo: Qwen/Qwen3-8B
      revision: main
      source: HF
    max_batch_size: 128
    max_num_tokens: 32768
    max_seq_len: 32768
    num_builder_gpus: 1
    plugin_configuration:
      use_fp8_context_fmha: true
    quantization_type: fp8_kv
    speculator:
      enable_b10_lookahead: true
      lookahead_ngram_size: 8
      lookahead_verification_set_size: 1
      lookahead_windows_size: 1
      speculative_decoding_mode: LOOKAHEAD_DECODING
    tensor_parallel_count: 1
  runtime:
    enable_chunked_context: false
